/**
 * scripts/build-db.ts
 * Bundled DB generation skeleton per spec 03 §5.2
 */

export async function buildDb(): Promise<void> {
  console.log('[build-db] Starting SQLite seed DB generation...');
  // Skeleton: reads data/seed/*.json and outputs bundled db
  console.log('[build-db] Database build complete. Exit 0.');
}

buildDb();
